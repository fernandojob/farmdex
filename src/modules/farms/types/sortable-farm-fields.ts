export enum SortableFarmFields {
  id = 'id',
  name = 'name',
  createdAt = 'createdAt',
  updatedAt = 'updatedAt',
  totalArea = 'totalArea',
  plotCount = 'plotCount',
  foundationDate = 'foundationDate',
}

export enum SortOrder {
  asc = 'asc',
  desc = 'desc',
}
